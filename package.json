{
  "publisher": "komiyamma",
  "name": "commit-message-gene-by-codex",
  "displayName": "commit-message-gene-by-codex",
  "description": "Generates commit messages in codex environments. It is intended for use when 'Github Copilot' is no longer available or with 'AWS kiro'.",
  "version": "0.3.15",
  "repository": {
    "type": "git",
    "url": "https://github.com/komiyamma/vscode_extension_commit_message_gene_by_codex"
  },
  "license": "MIT",
  "homepage": "https://komiyamma.net/?page=page-vscode-extension-commit-message-gene-by-codex",
  "engines": {
    "vscode": "^1.100.0"
  },
  "icon": "resources/icons/logo.png",
  "categories": [
    "SCM Providers"
  ],
  "activationEvents": [
    "onCommand:commit-message-gene-by-codex.runCodexCmd"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "commit-message-gene-by-codex.runCodexCmd",
        "title": "Commit message generation by codex",
        "icon": "$(sparkle-filled)"
      }
    ],
    "menus": {
      "scm/inputBox": [
        {
          "command": "commit-message-gene-by-codex.runCodexCmd",
          "group": "navigation@-100",
          "when": "scmProvider == git"
        }
      ],
      "scm/title": [
        {
          "command": "commit-message-gene-by-codex.runCodexCmd",
          "group": "navigation@100",
          "when": "scmProvider == git"
        }
      ]
    },
    "configuration": {
      "title": "Commit Message Gene by Codex",
      "properties": {
        "commitMessageGene.prompt.intro.en": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "markdownDescription": "English instructions shown to Codex before the Git context. Each array item becomes a paragraph with a blank line between entries.",
          "default": [
            "You are an assistant that drafts commit messages using the provided Git information.",
            "All required Git data has already been collected below. Do not run additional git commands.",
            "Follow the Conventional Commits style (type(scope?): subject) for the summary line and add a body only if it helps explain the change. Write the message in English. Do not use Markdown syntax; write in plain text.",
            "Return only the final commit message proposal."
          ]
        },
        "commitMessageGene.prompt.intro.ja": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "markdownDescription": "Japanese instructions shown to Codex before the Git context. Each array item becomes a paragraph with a blank line between entries.",
          "default": [
            "あなたは収集されたGit情報でコミットメッセージを作成するアシスタントです。",
            "必要なGitデータはすべて下に用意済みです。追加のgitコマンドは実行しないでください。",
            "サマリー行はConventional Commitsスタイル（type(scope?): subject）に従い、必要な場合のみ本文を追加してください。コミットメッセージは日本語で記述してください。Markdown表記は使わずプレーンなテキストで記述してください。",
            "最終的なコミットメッセージ案だけを返してください。"
          ]
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.100.0",
    "@typescript-eslint/eslint-plugin": "^8.39.0",
    "@typescript-eslint/parser": "^8.39.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.32.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.0",
    "@openai/codex-sdk": "^0.87.0"
  }
}
